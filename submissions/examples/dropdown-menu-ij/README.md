# ease-dropdown-menu

A dropdown menu whose trigger bar fills with a highlight while the options panel drops open.

## Run
Open `demo.html` directly in a browser to watch the list drop.

## Notes
- The trigger fills on the open phase.
- Each option staggers in with a fade.