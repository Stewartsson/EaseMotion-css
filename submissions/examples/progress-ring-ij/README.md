# ease-progress-ring

A progress ring whose arc strokes around the circle while the percentage label ticks inside.

## Run
Open `demo.html` directly in a browser to watch the ring fill.

## Notes
- The arc uses a stroked SVG circle.
- The track ring stays dark behind it.