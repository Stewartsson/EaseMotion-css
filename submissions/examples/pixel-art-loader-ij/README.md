# ease-pixel-art-loader

A pixel art loader whose blocks bounce one by one to assemble a sprite on a three-by-three grid.

## Run
Open `demo.html` directly in a browser to watch the sprite build.

## Notes
- Blocks bounce in a corner-to-corner order.
- The caption blinks below the grid.