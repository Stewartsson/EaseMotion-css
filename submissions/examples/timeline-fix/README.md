# Timeline Overlap Fix

## What it does
This component example fixes layout issues where timeline items and markers overlap on smaller mobile viewports.

## How to use it
Include `style.css` alongside `demo.html` to render a clean, responsive timeline structure.