# ease-metronome-tick

A metronome whose pendulum arm swings side to side while the weight slides to set the tempo and the BPM readout pulses.

## Run
Open `demo.html` directly in a browser to watch the arm swing.

## Notes
- The weight travels with the arm.
- The BPM pulses with each click.