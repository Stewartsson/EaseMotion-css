# ease-colormix-theming-bh

CSS color-mix() Dynamic Theming System for EaseMotion Components.

## What does this do?
Uses color-mix() to compute hover/active states from a single base color.

## Features
- Dynamic color variants using color-mix()
- Primary, secondary, success, warning, danger variants
- Hover, active, soft background states
