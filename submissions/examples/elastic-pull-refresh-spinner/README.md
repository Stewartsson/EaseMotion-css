# elastic-pull-refresh-spinner

An interactive pull-to-refresh spinner with continuous rotation animations.

## Features
- Pure CSS layout logic.
- Light and dark theme adaptable.
- Clean semantic HTML structure.
