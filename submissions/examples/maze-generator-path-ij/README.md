# ease-maze-generator-path

A maze generator whose walls build across the grid while the solution path grows to the exit.

## Run
Open `demo.html` directly in a browser to watch the path find its way.

## Notes
- The solution path lights up cell by cell.
- The exit marker blinks when reached.