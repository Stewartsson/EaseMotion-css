# ease-nav-hamburger-menu

A navigation menu whose hamburger morphs into a close X while the panel slides in and links stagger.

## Run
Open `demo.html` directly in a browser to watch the menu open.

## Notes
- The three bars morph into an X.
- Menu links slide in one after another.