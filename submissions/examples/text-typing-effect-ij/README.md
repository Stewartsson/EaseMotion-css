# ease-text-typing-effect

A terminal window where the command types in letter by letter, the prompt line waits above, and the caret blinks at the edge.

## Run
Open `demo.html` directly in a browser to watch the command type.

## Notes
- Text types in one character at a time.
- The caret blinks on and off.