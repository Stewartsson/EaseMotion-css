# Visual Regression Test Harness (Playwright)

This project demonstrates **visual regression testing** for CSS `ease-*` classes using Playwright.

## 🚀 Setup
1. Install dependencies:
   ```bash
   npm install -D @playwright/test