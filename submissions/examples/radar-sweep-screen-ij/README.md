# ease-radar-sweep-screen

A radar scope whose sweep line turns around the concentric range rings while the contact blips blink inside the sweep.

## Run
Open `demo.html` directly in a browser to watch the sweep turn.

## Notes
- The sweep rotates continuously.
- Blips blink inside the sweep.